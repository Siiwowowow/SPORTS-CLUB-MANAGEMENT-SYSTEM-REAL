import React from 'react';

const PendingBookings = () => {
    return (
        <div>
            <h1 className="text-3xl">Pending Bookings</h1>
        </div>
    );
};

export default PendingBookings;